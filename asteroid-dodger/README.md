# Asteroid Dodger

A tiny HTML5 canvas game: dodge falling asteroids and survive as long as you can.

## Play locally
Just open `index.html` in your browser, or use a small server:

```bash
# Python
python -m http.server 8080
Then open http://localhost:8080

Controls
Left: ← or A
Right: → or D
Start/Restart: Space